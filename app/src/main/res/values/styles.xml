<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonStyle" >
        <item name="android:layout_width">@dimen/button_dimen</item>
        <item name="android:layout_height">@dimen/button_dimen</item>
        <item name="android:paddingBottom">@dimen/header_margin</item>
    </style>

    <style name="ButtonStyleLeft" parent="ButtonStyle">
        <item name="android:background">@drawable/ic_chevron_left_black_35dp</item>
    </style>

    <style name="ButtonStyleRight" parent="ButtonStyle">
        <item name="android:background">@drawable/ic_chevron_right_black_35dp</item>
    </style>

    <style name="HeaderStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/headerTextColor</item>
        <item name="android:textSize">@dimen/header_text_size</item>
        <item name="android:layout_marginTop">@dimen/header_margin</item>
        <item name="android:layout_marginBottom">@dimen/header_margin</item>
        <item name="android:layout_centerHorizontal">true</item>
    </style>


    <style name="TabTextStyle">
        <item name="android:textSize">@dimen/tab_text_size</item>
    </style>


</resources>
